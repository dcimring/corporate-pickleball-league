// Fallback season label, shown only until the first results import arrives.
// The live label comes from the latest import (`data.season`), which the
// Apps Script sets from its SEASON property.
export const SEASON_LABEL = 'Summer 2026';
